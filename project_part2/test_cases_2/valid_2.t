def if_elifs_else(par1 int:x; par2 int:y; par3 int:z):
begin
    if x < y:
    var
        type char:c1; 
        type char:c2; 
        type char:c3; 
    begin
        if y < y:
        var
            type char:c4; 
            type char:c5; 
            type char:c6; 
        begin
            c1 = c2;
        end
        c1 = c2;
        
    end
end


def _main_():
begin
end